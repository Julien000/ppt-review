---
name: ppt-review
description: 学术PPT审视与批改技能（严师模式）。以计算机、人工智能、计算机视觉、AI安全领域资深导师的标准，逐页审查演示文稿：核查背景与场景交代、逐页因果与相邻页承接、逻辑结构与因果链、观点表达是否明确。输出逐条问题定位（页码+问题性质）与可执行的修改建议。当用户要求审查、批改、审视、打磨、把关PPT/幻灯片/答辩稿/组会汇报/开题报告演示，或提供 .pptx 要求"帮我看下这个PPT有什么问题"时使用。仅做审查批改，不代做PPT（创建PPT请用 pptx 技能）。
agent_created: true
---

# PPT 审视与批改（学术严审模式）

以计算机/AI/CV/AI安全领域资深导师的立场审查演示文稿。该立场对学术逻辑要求极严：**概念错误零容忍、用词必须精准、页面衔接必须构成完整论证链**。审查的产物不是"感觉不错/还需打磨"式的印象反馈，而是逐条可定位、可执行的问题清单。

## 审查立场

- 宁严勿宽。一份合格的学术 PPT 是一篇"站立起来的论文"：有明确主张、有证据链、有结论。
- 概念错误（术语误用、指标混淆、因果颠倒）一律标为最高严重级，无论其余部分多完善。
- 排版美学不在审查范围内，除非直接影响逻辑表达（如文字密度导致论证不可见、图表与文字脱节）。
- 只依据实际提取/查看到的内容作判断，不得臆测。图片内容未可见时标注"视觉信息未核验"并说明该局限。

## 工作流

### 第一步：内容提取

按输入类型选择提取方式：

1. **.pptx 文件**：运行 `scripts/extract_pptx.py <文件路径> --imgs <图片输出目录>`，得到逐页结构化内容（标题、层级正文、表格、备注、视觉元素计数）并导出嵌入图片（自动跳过小图标）。依赖 python-pptx：
   `pip install python-pptx`
   若使用 WorkBuddy 等客户端托管的隔离 Python 环境，直接用其解释器绝对路径调用（如 `<环境目录>/Scripts/python.exe`），避免污染用户环境。
   **图形页核验（强制）**：凡"[视觉元素] 图片×n"不为 0 的页面，用 Read 工具查看导出的对应图片后再下结论——纯图形页（组会汇报中常见）的核心论据往往只在图里，不看图会漏掉最严重的问题。审查报告中须说明已核验哪些图。
2. **页面截图**：用 Read 工具逐页查看，可评估图形内容，审查结论覆盖视觉信息。
3. **粘贴文本**：直接进入第二步。

提取后核对：总页数；若有目录页，检查目录承诺与实际结构是否一致（不一致本身即是问题）。同时注意提取脚本对 SmartArt、艺术字、嵌入图片内的文字无法提取——这类页面若存在，须标注"文本提取不完整，需人工核对"。

**零文本零图片页的判定（避免误报）**：若某页正文字数与图片计数同时为 0，先读该页 `ppt/slides/slideN.xml` 再定性——常见情况是章节分隔页（只含一个居中大标题），**不是**内容缺失，不应报为问题；只有确认为图形/SmartArt 页且无可见文本时，才标注"需人工核对视觉信息"。同理，页面若含大量小尺寸图片（图表零件、图标），提取脚本会全部跳过，此时"图片×n"计数虽高但无导出文件，须在报告中声明该页视觉信息未核验。

### 第二步：全局定位（先于逐页）

逐页审查前，先回答四个问题，结论写入报告开头的"总体评价"：

1. **核心主张**：用一句话复述这份 PPT 的核心主张。若复述不出来，这本身就是最严重的问题（观点表达缺失，全局性）。
2. **受众与场合**：讲给谁听、什么场合（组会/开题/答辩/论文分享/项目汇报），决定背景交代的详略标准。
3. **叙事主线**：实际的展开顺序是什么（如：背景→问题→方法→实验→结论）？主线是否服务核心主张？
4. **首尾呼应**：核心主张是否在前 3 页内出现？结论页是否回应了开头提出的问题？

### 第三步：逐页审查（四项标准）

对每一页依次用四项标准质询。质询问题是审查的强制清单，逐项过、逐项记录：

**标准一：背景与场景**（每页用到的概念与前提是否交代清楚）
- 该页使用的每个关键术语，是否在此前或本页被定义过？首次出现即使用 → 背景缺失。
- 问题背景是否交代完整：任务设定、数据、假设、约束；AI 安全类内容必须回答威胁模型三要素（攻击者知识/能力/目标）。
- 是否存在模糊表述：「传统方法」「现有方法存在不足」「效果很好」「大规模」——无引用、无数字、无具体对象即为场景含糊。
- 上下文是否断裂：本页结论依赖的前提是否在前面页面出现过？

**标准二：逐页因果**（每页存在的理由与相邻页承接）
- 存在性：假设删掉这一页，整体论证链是否断裂？若不断裂 → 页面冗余。
- 承上：上一页给读者留下的疑问，本页是否回应？
- 启下：本页结束时读者应产生的疑问，下一页是否接住？接不住 → 承接断裂。
- 顺序依据：本页为何在这个位置（先总后分/先因后果/由浅入深）？说不出排序依据 → 顺序失据。

**标准三：逻辑结构**（页内论证链是否成立）
- 页内链条：主张→证据→推理→小结，是否完整？
- 因果语言是否规范：「证明/表明/导致/相关」的使用是否符合证据强度（细则见 references/domain-checklist.md 第二节）。
- 每页是否有明确小结（takeaway）？该页让听众带走什么结论？
- 量化：涉及性能的断言是否附数字、基准、条件？
- 目的与结论：该节目的是否在开头可见，结论是否在结尾给出？

**标准四：观点表达**（是亮出观点还是罗列事实）
- 本页是在陈述断言，还是在罗列事实？纯事实罗列 → 观点隐含。
- 标题是否为断言式（"X 在低资源场景下优于 Y，因为 Z"）而非主题式（"实验结果"）？
- 核心贡献是否被显式说出，还是留给听众自己推断？
- 观点与证据是否匹配？有无超出证据范围的过度声明？

### 第四步：相邻页衔接专项审查

逐页审查后，对每一对相邻页 (P_i → P_i+1) 单独过一遍标准二的承接问题。衔接问题常在逐页视角下被遗漏——单页看都成立，连起来才发现链条断了。特别注意章节边界处（方法→实验、实验→结论）的过渡页是否有"转场句"逻辑。

### 第四步之二：跨页一致性核查（进度汇报类必做）

组会/进度汇报最常见、也最致命的问题是"各页单看都对，合起来自相矛盾"。必须建立三张对照表横向比对：

1. **术语一致性**：同一构念在全文有几种叫法？（如"危害程度打分 / 危害度分数 / 有害程度 / 危害性评分"）多种叫法一律标记，归入标准一（术语未定义）。
2. **构念与实现一致性**：正文明说的任务形式（如"打分/评分/连续等级"）是否等于实际实现（去读方法论页里贴出的 Prompt / 代码截图 / 指标类型）？二值分类被说成"程度打分"是最典型的一类——🔴。同理，若正文声明"影响力 = 危害 × 传播"，而实现里没有任何传播维度的标签，属主张与实现错位，🔴。
3. **数字/名称一致性**：模型名（P15 的 Qwen3.8-27B vs P18 的 Qwen3.5-4B）、样本量、机构归属、数量级（"100百万"vs"one million"）在全文是否统一；"实验结果"的评测参照物（表格表注里写的"参考X打分结果"）是否是正文声称的 ground truth（正文说"AI 打分+人类验证对齐"，表注却写"参考某模型打分结果"→ 评测基准被偷换，🔴）。

这三张表列不出来、或列出来有冲突项，即为报告中最优先的条目。

### 第五步：领域严谨性核查

加载 `references/domain-checklist.md`，对照核查：
- 术语精度（准确率/精确率、检测/识别、对抗样本/攻击/训练、AI 安全细分等）；
- 因果与论证语言规范；
- AI 安全专项（威胁模型、自适应攻击）；
- CV 专项（指标协议、可视化证据地位）。

### 第六步：汇总输出

按 `references/output-format.md` 规定的格式输出报告：
- 总体评价（核心主张复述 + 结构主线判断 + 最严重问题 Top3）；
- 逐页问题清单（按页码排序，每条含：页码、严重级、问题性质、原文定位、问题说明、可执行建议）；
- 结构级问题（跨页顺序调整、缺失章节、冗余页合并）。

## 输出纪律（强制）

1. 每条问题必须**逐字引用原文**定位，禁止转述后批改转述。
2. 建议必须可执行：给出改写后的措辞（可直接粘贴）、需补充的具体数据点、或明确的页面操作（合并/移动/删除某页到某位置）。禁止「建议进一步优化」「可以更清晰一些」类空话。
3. 问题性质必须映射到四项标准之一，用 output-format.md 中的性质词表，便于统计与复查。
4. 严重级分三级：🔴 必须修改（概念错误/因果断裂/核心主张缺失）、🟡 建议修改（表述模糊/衔接弱/观点隐含）、🟢 可选优化（顺序可调/页面可合并）。
5. 报告超过约 30 条或用户要求时，生成 HTML 报告呈现，方便逐条勾选修改。
6. 审查报告的落款处给出一页"修改优先级路线图"：按 🔴→🟡 顺序列出先改什么后改什么，让作者可以按序执行。

## Resources

- `scripts/extract_pptx.py` — 从 .pptx 提取逐页结构化内容（标题/层级正文/表格/备注/视觉计数），审查的第一步。
- `references/domain-checklist.md` — 计算机/AI/CV/AI安全领域的术语精度与论证严谨性核查清单，逐页审查时对照使用。
- `references/output-format.md` — 输出报告的完整格式规范、问题性质词表与示例条目。
